<?xml version="1.0" encoding="utf-8"?>
<manifest
    package="com.sevenheroes"
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET"/>

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/Theme.AppCompat.Light.DarkActionBar">
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name">
        </activity>
        <activity android:name=".WelcomeActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>

                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <activity
            android:name=".LoginActivity"
            android:label="@string/title_activity_login"
            android:theme="@android:style/Theme.Holo"
            tools:ignore="NewApi">
        </activity>
        <activity
            android:name=".ServerActivity"
            android:theme="@android:style/Theme.Holo"
            tools:ignore="NewApi"/>
        <activity
            android:name=".CommonActivity"
            android:label="@string/title_activity_common"
            android:theme="@style/StyledIndicators">
            <intent-filter>
                <action android:name="com.sevenheroes.CommonActivity"/>
                <category android:name="android.intent.category.DEFAULT"/>
            </intent-filter>
        </activity>
        <activity
            android:name=".FloorFightingActivity"
            android:label="@string/title_activity_floor_fighting"
            android:theme="@android:style/Theme.Holo"
            tools:ignore="NewApi">
            <intent-filter>
                <action android:name="com.sevenheroes.FloorFightingActivity"/>
                <category android:name="android.intent.category.DEFAULT"/>
            </intent-filter>
        </activity>
        <activity
            android:name=".ChooseLeaderActivity"
            android:label="@string/title_activity_choose_leader"
            >
        </activity>
    </application>

</manifest>
